# Used only by Next.js at dev/build time (rewrites in next.config.mjs). Default: http://localhost:4000
# BACKEND_URL=http://localhost:4000

# Exposed to the browser: direct API origin for binary file downloads (same host/port as BACKEND_URL).
# NEXT_PUBLIC_BACKEND_URL=http://localhost:4000
