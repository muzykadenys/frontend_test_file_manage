# Dev-only: where Next proxies /backend-api/* (see next.config.mjs). Not exposed to the browser bundle.
BACKEND_URL=http://localhost:4000

# Browser must fetch file bytes directly from the API (bypasses Next rewrites; fixes empty/broken downloads).
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000
